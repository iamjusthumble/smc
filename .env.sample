VITE_DEV_ASSET_URI=
VITE_DEV_APOLLO_URI=
VITE_SENTRY_DSN=
VITE_TEST_APOLLO_URI=
VITE_TEST_ASSET_URI=
VITE_PROD_ASSET_URI=

VITE_PROD_APOLLO_URI=
VITE_NODE_ENV=

# Backend is currently down. Defaults to true (mock data, no network calls).
# Set to false once a real backend is available again.
VITE_MOCK_BACKEND=

# Supabase project used for authentication only (see src/context/auth-context.tsx).
# Required — the app throws loudly at startup if either is missing.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=